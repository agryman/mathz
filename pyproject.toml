[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mathz"
version = "0.0.1"
readme = "README.md"
authors = [
    { name = "Arthur Ryman", email = "arthur.ryman@gmail.com" }
]
